const chai = require('chai');
const Source = require('../lib');

chai.should();

describe('dataplug-cli', () => {
  describe('#()', () => {
    it('', () => {});
  });

  describe('#createOutput()', () => {
    it('', () => {});
  });
});
